Import('env')

src = [
    "main.cc",
    "LogManager.cc",
]

env.Program("dlogd", src,
            LIBPATH = [ "#build/libDLogRPC", "#build/libDLogStorage" ],
            LIBS = [ "DLogRPC", "DLogStorage" ],
            CPPPATH = "#include")

