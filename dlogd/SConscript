Import('env')

src = [
    "main.cc",
    "AsyncMutex.cc",
    "Debug.c",
    "LogManager.cc",
    #"#build/proto/dlog.pb.o",
]

env.Program("dlogd", src,
            LIBPATH = [ "$LIBPATH",
                        "#build/libDLogRPC",
                        "#build/libDLogStorage" ],
            LIBS = [ "event_core-2.0", "DLogRPC", "DLogStorage" ],
            CPPPATH = [ "#include", "#" ])

