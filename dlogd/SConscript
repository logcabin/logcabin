Import('env')

src = [
    "main.cc",
    "LogManager.cc",
    "#build/proto/dlog.pb.o",
]

env.Program("dlogd", src,
            LIBPATH = [ "#build/libDLogRPC", "#build/libDLogStorage" ],
            LIBS = [ "protobuf", "DLogRPC", "DLogStorage" ],
            CPPPATH = [ "#include", "#" ])

